# Required for LLM extraction and clustering (omit to use --heuristic mode)
ANTHROPIC_API_KEY=sk-ant-...

# Optional: raises GitHub API limits and enables access to authorized private repositories
GITHUB_TOKEN=github_pat_...
